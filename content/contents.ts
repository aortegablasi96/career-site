import type { Contents } from './types';

/**
 * The page's contents bar, per DDR-031. Only its accessible name is here: the word each link
 * shows sits in its own section's module, beside that section's heading.
 */
export const contents: Contents = {
  label: 'Sections',
};
