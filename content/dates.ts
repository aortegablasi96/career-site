import type { DateLabels } from './types';

/** How dates read on the page: English three-letter months, per DDR-006, and the end of a current role. */
export const dateLabels: DateLabels = {
  months: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
  present: 'Present',
};
