import type { Placeholder } from './types';

/**
 * Temporary copy, present so the scaffold has a page to render. It is not site content
 * and carries no professional claims. Delete this module once real content exists.
 */
export const placeholder: Placeholder = {
  heading: 'Andreu Ortega Blasi',
  message: 'This site is being built.',
};
